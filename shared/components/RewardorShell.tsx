"use client";
import Link from "next/link";
import { BarChart3, ChevronDown, Gift, LayoutDashboard, Megaphone, Settings, ShieldCheck, Users } from "lucide-react";
import { usePathname } from "next/navigation";
const nav = [{ href:"/", label:"Overview", icon:LayoutDashboard }, { href:"/campaigns", label:"Campaigns", icon:Megaphone }, { href:"/claims", label:"Claims & redemptions", icon:Gift }, { href:"/analytics", label:"Analytics", icon:BarChart3 }, { href:"/settings", label:"Settings", icon:Settings }];
export function RewardorShell({ children }: { children: React.ReactNode }) { const path = usePathname(); return <div className="min-h-screen lg:flex"><aside className="hidden w-72 shrink-0 border-r border-border bg-surface-card lg:flex lg:flex-col"><div className="flex h-20 items-center gap-3 border-b border-border px-7"><div className="flex h-10 w-10 items-center justify-center rounded-2xl bg-primary text-primary-foreground"><Gift size={21}/></div><div><p className="m-0 text-lg font-black tracking-tight text-foreground-heading">KOMOLA</p><p className="m-0 text-xs font-semibold text-foreground-muted">Rewardor workspace</p></div></div><div className="border-b border-border p-5"><button className="flex w-full items-center gap-3 rounded-2xl border border-border bg-surface px-3 py-3 text-left"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-secondary font-bold text-primary">SF</div><div className="min-w-0 flex-1"><p className="m-0 truncate text-sm font-bold text-foreground-heading">Sahaja Foods</p><p className="m-0 text-xs text-foreground-muted">Rewardor account</p></div><ChevronDown size={16} className="text-foreground-muted"/></button></div><nav className="flex-1 space-y-1 p-5">{nav.map(({href,label,icon:Icon}) => <Link key={href} href={href} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold transition ${path===href||path.startsWith(`${href}/`) ? "bg-primary text-primary-foreground" : "text-foreground-muted hover:bg-surface hover:text-foreground-heading"}`}><Icon size={18}/>{label}</Link>)}<div className="my-5 border-t border-border"/><div className="flex items-center gap-3 px-3 py-3 text-xs font-semibold uppercase tracking-wider text-foreground-muted"><ShieldCheck size={16}/> Moderated workspace</div></nav><div className="border-t border-border p-5"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-secondary text-sm font-bold text-primary">AR</div><div><p className="m-0 text-sm font-bold text-foreground-heading">Aarav Rao</p><p className="m-0 text-xs text-foreground-muted">Owner</p></div></div></div></aside><main className="min-w-0 flex-1">{children}</main></div> }
