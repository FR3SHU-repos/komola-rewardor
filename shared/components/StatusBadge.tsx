import type { CampaignStatus } from "../data/demo";
export function StatusBadge({ status }: { status: CampaignStatus | string }) { const cls = status === "Published" || status === "Redeemed" ? "bg-status-success-surface text-status-success" : status === "Paused" || status === "Claimed" ? "bg-status-warning-surface text-status-warning" : status === "Expired" ? "bg-status-danger-surface text-status-danger" : "bg-status-info-surface text-status-info"; return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-bold ${cls}`}>{status}</span>; }
