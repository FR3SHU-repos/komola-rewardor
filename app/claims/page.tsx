import { Download, Search } from "lucide-react";
import { RewardorShell } from "@/shared/components/RewardorShell";
import { StatusBadge } from "@/shared/components/StatusBadge";
import { recentClaims } from "@/shared/data/demo";
export default function Claims(){return <RewardorShell><header className="flex flex-wrap items-center justify-between gap-4 border-b border-border bg-surface-card px-5 py-5 sm:px-8"><div><p className="m-0 text-sm font-semibold text-foreground-muted">Campaign activity</p><h1 className="m-0 mt-1 text-2xl font-black text-foreground-heading">Claims & redemptions</h1></div><button className="inline-flex items-center gap-2 rounded-xl border border-border bg-surface-card px-4 py-2.5 text-sm font-bold text-foreground-heading"><Download size={16}/> Export</button></header><div className="space-y-6 p-5 sm:p-8"><div className="grid gap-4 sm:grid-cols-3">{[["Total claims allowed","617"],["Redeemed","284"],["Pending Komola points","18,420"]].map(([l,v])=><div className="rounded-2xl border border-border bg-surface-card p-5" key={l}><p className="m-0 text-sm font-semibold text-foreground-muted">{l}</p><p className="m-0 mt-2 text-3xl font-black text-foreground-heading">{v}</p></div>)}</div><div className="overflow-hidden rounded-2xl border border-border bg-surface-card"><div className="flex items-center gap-3 border-b border-border p-4"><Search size={17} className="text-foreground-muted"/><input className="w-full bg-transparent text-sm outline-none" placeholder="Search claim code or buyer"/></div><div className="hidden grid-cols-[130px_1.5fr_1fr_140px_150px] gap-4 border-b border-border bg-surface p-4 text-xs font-bold uppercase tracking-wider text-foreground-muted md:grid"><span>Code</span><span>Campaign</span><span>Buyer</span><span>Status</span><span>Date</span></div>{recentClaims.map(c=><div key={c.code} className="grid gap-2 border-b border-border p-5 last:border-0 md:grid-cols-[130px_1.5fr_1fr_140px_150px] md:items-center md:gap-4 md:p-4"><span className="font-mono text-sm font-bold text-primary">{c.code}</span><span className="text-sm font-bold text-foreground-heading">{c.campaign}</span><span className="text-sm text-foreground-muted">{c.buyer}</span><span><StatusBadge status={c.status}/></span><span className="text-sm text-foreground-muted">{c.date}</span></div>)}</div></div></RewardorShell>}
