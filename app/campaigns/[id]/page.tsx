import Link from "next/link";
import { ArrowLeft, BarChart3, Gift, Pause, Pencil, Users } from "lucide-react";
import { RewardorShell } from "@/shared/components/RewardorShell";
import { StatusBadge } from "@/shared/components/StatusBadge";
import { campaigns } from "@/shared/data/demo";
export default async function CampaignDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const campaign = campaigns.find(c => c.id === id) ?? campaigns[0]; return <RewardorShell><header className="border-b border-border bg-surface-card px-5 py-5 sm:px-8"><Link href="/campaigns" className="inline-flex items-center gap-2 text-sm font-bold text-foreground-muted"><ArrowLeft size={16}/> Back to campaigns</Link><div className="mt-5 flex flex-wrap items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-3"><h1 className="m-0 text-2xl font-black text-foreground-heading">{campaign.title}</h1><StatusBadge status={campaign.status}/></div><p className="m-0 mt-2 text-sm text-foreground-muted">{campaign.reward} · {campaign.category} · Ends {campaign.ends}</p></div><div className="flex gap-2"><button className="inline-flex items-center gap-2 rounded-xl border border-border px-4 py-2.5 text-sm font-bold text-foreground-heading"><Pencil size={16}/> Edit</button>{campaign.status === "Published" && <button className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground"><Pause size={16}/> Pause</button>}</div></div></header><div className="space-y-6 p-5 sm:p-8"><div className="grid gap-4 sm:grid-cols-3">{[["Claims",String(campaign.claims),Users],["Remaining",String(campaign.limit-campaign.claims),Gift],["Conversion","46%",BarChart3]].map(([label,value,Icon])=><div className="rounded-2xl border border-border bg-surface-card p-5" key={String(label)}><div className="flex justify-between"><p className="m-0 text-sm font-semibold text-foreground-muted">{label as string}</p><Icon size={18} className="text-primary"/></div><p className="m-0 mt-3 text-3xl font-black text-foreground-heading">{value as string}</p></div>)}</div><section className="rounded-2xl border border-border bg-surface-card p-6"><h2 className="m-0 text-lg font-black text-foreground-heading">Campaign readiness</h2><div className="mt-5 space-y-4">{[["Campaign details","Complete"],["Eligibility rules","Configured"],["Claim limits","Configured"],["POS redemption","Ready"]].map(([label,value])=><div className="flex items-center justify-between border-b border-border pb-4 last:border-0 last:pb-0" key={label}><span className="text-sm font-semibold text-foreground-heading">{label}</span><span className="text-sm font-bold text-status-success">{value}</span></div>)}</div></section></div></RewardorShell> }
